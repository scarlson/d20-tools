from models.ChatUser import ChatUser
import hashlib
from base64 import b64decode, b64encode

def login(username):
    pass


def PassCheck(pass):
    m = hashlib.sha1()
    m1 = hashlib.sha1()
    s = b64decode(salt)
    

    if
        return false
    else:
        return true